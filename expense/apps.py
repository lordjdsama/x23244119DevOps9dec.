"""
This module contains the application configuration for the 'expense' app.
"""

from django.apps import AppConfig


class ExpenseConfig(AppConfig):
    """
    Configuration for the 'expense' app.
    """
    name = 'expense'  # pylint: disable=too-few-public-methods
